# Frontend environment variables
# Copy this file to .env for local development.
# In production, set VITE_API_URL in the Vercel dashboard (Project → Settings → Environment Variables).

# URL of your deployed Render backend (no trailing slash)
# Example: https://skillvouch-backend.onrender.com
VITE_API_URL=https://your-render-backend.onrender.com

# For local development only (Vite dev server proxies /api → localhost:3000 automatically,
# so you do NOT need to set VITE_API_URL in .env for local dev)
